{
  "name": "turkaffe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build":  "open-cli index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "open": "^10.1.2"
  },
  "devDependencies": {
    "open-cli": "^8.0.0"
  }
}
